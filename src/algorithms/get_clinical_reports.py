"""Placeholder for the unrecovered clinical-report processing module.

The original project referenced a `GetClinicalReports` algorithm, but the complete source
was not preserved in the surviving materials. This stub keeps the repo structure coherent
and documents the missing piece without pretending the original implementation is available.
"""

import pandas as pd


class GetClinicalReports:
    def startClinicalReports(self):
        return pd.DataFrame(
            [
                {
                    "status": "partial-archive",
                    "message": "Original clinical-report processing code was not recovered.",
                }
            ]
        )
